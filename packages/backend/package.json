{
  "name": "@ts-template/backend",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index",
  "typings": "dist/index",
  "scripts": {
    "test": "jest",
    "start": "node dist/index.js",
    "clean": "rimraf dist",
    "build": "npm run -s clean && tsc",
    "dev": "ts-node-dev --deps --respawn src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.7",
    "ts-node-dev": "^1.0.0-pre.57"
  },
  "dependencies": {
    "express": "^4.17.1",
    "@ts-template/common": "^0.0.0"
  },
  "jest": {
    "preset": "ts-jest"
  }
}
